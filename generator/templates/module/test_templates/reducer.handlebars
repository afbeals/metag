// External
import { expect } from 'chai';

// Internal
import reducer from '../reducer';
import actions from '../actions';
{{#if util}}import util from '../util';{{/if}}

const { {{name}}: {} } = actions;
const {{camelName}}ReducerTest = () => describe('Reducer', () => {
    let initialStore; // instantiate beforehand
    const mockStore = util.buildMockStore;
    beforeEach(() => {
        {{#if util}}
            initialStore = util.buildInitialStore(); // assign for each test block
        {{else}}
            initialStore = {};
        {{/if}}
    });

    it('Should have initial store', () => {
        expect(
            reducer(undefined, {})
        ).to.deep.equal(initialStore);
    });
});

export default {{camelName}}ReducerTest;
