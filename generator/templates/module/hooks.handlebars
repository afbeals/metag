// External
import { useEffect, useState, useRef } from 'react';
import { useDispatch, useSelector } from 'react-redux';
import { checkPropTypes, object, func } from 'prop-types';

// Internal

// Constants
const {{camelName}}Hooks = {
  /**
   * @method hook
   * @desc an hook
   */
  hook: () => {},
};

export default {{camelName}}Hooks;
