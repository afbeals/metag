// External
import { useEffect, useState, useRef } from 'react';

// Internal

// Constants
const {{camelName}}Hooks = {
  /**
   * @method hook
   * @desc an hook
   */
  hook: () => {},
};

export default {{camelName}}Hooks;
