// External

// Internal
{{#if util}}
import util from './util';
{{/if}}
import { createActions } from '~GlobalUtil/normalize';

// Constants
const MODULE = `${process.env.REACT_APP_MODULE_NAME}/{{upperName}}/`; // module action prefix

// Actions
/**
 * Name
 *
 * @method
 * @name path
 * @desc desc
 *
 */
const actions = createActions(
  {
    {{name}}: {},
  },
  { prepend: MODULE },
);

export default actions;
